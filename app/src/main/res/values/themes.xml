<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MedicaApp" parent="Theme.Material3.DayNight.NoActionBar">
    </style>

    <style name="Theme.MedicaApp" parent="Base.Theme.MedicaApp" />
    
    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/md_theme_inversePrimary_highContrast</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/logo</item>
        <item name="windowSplashScreenAnimationDuration">4000</item>
        <item name="postSplashScreenTheme">@style/Theme.MedicaApp</item>
    </style>
</resources>